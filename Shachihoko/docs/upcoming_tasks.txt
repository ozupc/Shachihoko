・ExportGLTFComponentにアニメーション機能を追加する。
・SolveInstance()のelse if(ExportStyle == 1)つまりアニメーションが有の時の挙動を作成する。
・Transform（Translate, Scale, Rotate）をRhino.Geometry.Matrixで入力する。（設定済み）
・GLTFをMatrix（System.Numerics.Matrix4x4）で動かす方法を調べ実装する。→Matrixで動かうことはできなさそうなので、行列を分解することを検討。

8/22
・(IEnumerable<(float, Vector3)>)scalesのキャストでエラー発生。→解決済み

8/23
・書き出しはできるようになったがアニメーションとしては機能していない。キーフレームになっていない？またアニメーションがScale, Rotate, Translateでそれぞれバラバラになってしまっている。

8/25
・Decomposeがうまく機能していないので調整中。

8/26
・一応動作するようになったが、移動の開始位置がおかしい？

8/27
・nodeが複数あるとOpenseaにアップできない？